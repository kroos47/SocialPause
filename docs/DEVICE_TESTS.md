# Galaxy S24 Ultra acceptance — 0.7.0

Build success does not certify Samsung behavior. Use the real phone with Accessibility, notifications and precise alarms enabled. Keep the existing signing key and install over the old app; do not uninstall merely to fix a signature mismatch.

1. **Upgrade:** existing history, app selection, Sleep Time, lunch and running state remain. Updating from 0.3/0.4/0.5/0.6 must preserve existing partial usage and cooldown deadlines. An already-running upgrade receives no retrospective Stop lock; verify its next Start begins six hours. Upgrading from 0.2 resets old timers once to 7/10 minutes. Reopen repeatedly: current usage must not reset again.
2. **Independent limits:** use Instagram for seven minutes. It redirects Home and starts a 60-minute cooldown immediately; X and Reddit remain available. Use X for ten minutes; its unlock time is ten minutes later than Instagram's. With Shared timer off, exceed twenty total minutes across apps without a shared cutoff.
3. **Pause:** leave a partially used app, switch to an unselected app, or lock the screen. Usage freezes. Return and resume the exact remainder. Cooldowns continue while locked. Opening the notification drawer or Quick Settings over a selected app must keep charging that app; close the drawer and verify no jump or icon switch. Opening the Settings app, Home or Recents pauses usage. Leave the drawer open through allowance exhaustion and verify Home redirection.
4. **Reset:** Instagram's cooldown expires even if no other app was used. Only Instagram resets. Keep another app focused during that transition and check its usage continues.
5. **Home and blocking:** inspect mixed and all-cooling states. Every card has its own timer; the mint summary shows availability/resting state. Reopening a cooling app returns Home. The overlay shows the correct 7/10-minute limit and unlock time and dismisses on lock.
6. **Notifications:** selected app focus shows its timer; Home/unselected focus shows the expanded overview. Check mixed/all-cooling rows, progress bars, and each separate timer. During active usage, check the requested app-icon/countdown status chip; otherwise verify the SocialPause icon without a timer chip. Opening the drawer must retain the active app notification. Try Clear all, individual swipe, lock/unlock, permission denial and regrant. A swipe should restore an ordinary notification without repeated alerts. Dismissing an ordinary lunch/cooldown/idle notification must allow later focused live promotion; dismissing an active live countdown suppresses promotion until the next Start. Stop and Sleep Time must prevent restoration. Samsung may control or suppress promotion; record what actually occurs.
7. **Sleep:** verify both overnight and same-day windows. Notifications disappear at the start and return after the end. Timers keep enforcing limits.
8. **Lunch:** scheduled or manual start grants a full unrestricted hour, then blocks all for one hour and restores allowances. Early Stop lunch starts a full blocked hour. Manual lunch can replace any cooldown, once per local day. Start it before auto lunch and check auto is skipped; after auto lunch, one extra manual start is available. Check disabled Start after use, Stop/Start, process recreation, reboot and midnight persistence. Confirm a pending edit cannot cut short an overnight lunch/cooldown.
9. **Insights Today:** only per-app used time, no chart/large total card. Lunch/Stop usage is excluded. Usage sums across independent cycles.
10. **Insights Week:** colored segments sum to each day's total. Tap each bar or empty day; total and By App change to that date while the chart remains weekly. Tap the same day again or elsewhere outside day targets to restore totals; selecting another day switches directly. Scrolling must keep selection. Future days are disabled. Rotate while selected, switch Today/Week, and test a new week boundary.
11. **Appearance/accessibility:** dark/light mode, large text, gesture/button navigation, landscape, TalkBack day selection, and notification expansion. Test Samsung split-screen/pop-up focus: only the focused unlocked app should consume usage.
12. **Lifecycle:** closing or swiping the dashboard from Recents does not stop monitoring. Ordinary process recreation keeps timers, history and the Stop lock. Reboot or a confirmed Android Force stop leaves monitoring stopped until Start; reboot may reset ordinary usage but preserves settings/history/lunch records. Stop permits free use once its six-hour lock ends; Start refreshes allowances and starts a fresh lock. Disable SocialPause Accessibility during usage, cooldown and lunch, including while Stop is locked: tracking stops, all notifications/overlays disappear, no alarm recreates them, and reopening shows Start disabled. Re-enable access: monitoring stays stopped until Start. Ordinary disconnection with permission still enabled must not reset timers or the lock.

Use fake-clock scenarios for exact boundaries; normal phone acceptance should also include a real cooldown and screen-off/idle interval. Do not shorten production constants to make a test pass. Collect only SocialPause-related logs; do not share private app data.

## Status-bar countdown regression

Use X briefly: drawer and Home counters must continue working. Verify the chip shows decreasing `MM:SS` with the X glyph when Samsung promotes it. Switch apps, open/close the drawer, then Home: the focused app stays stable through the drawer and idle has only the SocialPause icon. Check Instagram starts at 07:00 and others at 10:00.

The user confirmed the S24 Ultra displays the live countdown after enabling **Phone Settings → Developer options → Live notifications for all apps**. Keep that setting enabled; it affects compatible apps generally. If the number disappears, check that switch and the app-specific Live notifications controls in Samsung's notification/Now bar settings. Menu availability varies by firmware. Android's promotion flag alone does not prove a Samsung live pill is visible.

## Historical 0.5 acceptance

These scenarios record the original feature introduction. For a current install, use the 0.6 controls described below and the 0.7 Stop/recovery rules; configure tests while stopped or after Stop unlocks. Keep old version ranges and clock-picker checks only for regression fixtures of those releases.

- **Shared timer:** while stopped, toggle it on. Settings offers 00:05, 00:10, …, 01:00 (default 00:20). Save persists; Cancel/outside dismissal leaves the old value. Both controls explain why edits are unavailable while running.
- **Shared accounting:** use Instagram 7 minutes, X 10, Reddit 3 with 20-minute shared allowance. Each app limit starts its own cooldown; Reddit's third minute starts a 60-minute group block. All apps reset when it finishes. Separately, let an app's cooldown finish while shared time remains: only that app refills, shared time does not. Test 5-minute shared exhaustion before an app's own limit.
- **Shared presentation:** Home shows shared remaining above app cards. Active notification uses the smaller app/shared countdown and identifies the limiting allowance. The chip uses SocialPause's icon when shared time limits usage; idle and group cooldown show ordinary notifications with correct labels/rows. Drawer focus and Sleep Time behavior remain.
- **Schedule editing:** at 10 AM change 2 PM to 3 PM: applies today. After 2 PM or after a manual lunch, applies tomorrow. At 10 AM choose 9 AM: tomorrow, with today's existing schedule retained. Verify effective-date copy. Open clock picker, change draft, tap outside or Back: nothing changes. Save while boundary passes uses the save-time decision.
- **App picker:** Stop; tap Selected apps once and repeatedly while loading. Exactly one list opens. Add YouTube or another installed launcher app, Save, inspect Home, reopen picker and relaunch. Selection persists with a 10-minute allowance. Cancel discards; clearing all selections reports an error in the same dialog without saving.
- **Upgrade:** install 0.5 over 0.4 using the same signing key while an app is partly used, cooling down, or in lunch. Preserve history/settings/timers and initialize only new shared/manual fields. Keep the temporary status-bar diagnostic absent.


## Retained 0.6 hybrid acceptance (current controls; 0.7 Stop rules apply)

- Appearance: toggle sun/moon while stopped, during active usage, individual/shared cooldown and lunch. Inspect Home, Settings, picker/sheet and the next blocking overlay. Theme persists on relaunch; timers, history and manual quota remain unchanged. Inspect light/dark, 150% text, landscape and keyboard/TalkBack navigation.
- Mode: choose Individual / Shared while stopped. While running both are disabled with explanation. App sliders are editable only stopped, in either mode; shared slider only stopped in Shared. The sheet remains readable in Individual/running states.
- Allowances: save Instagram 0/1/7 and another app 0/1/12. Value labels are MM:SS; shared is HH:MM. Shared slider 00:01/00:20/00:30 uses one-minute steps and saves immediately; Done, Back and backdrop close without reverting. Per-app values survive mode/selection changes, Stop/Start, lunch, process recreation and reboot.
- Zero: select zero for one app and then every app. Launches are blocked with No allowance and no unlock deadline. No repeating cooldown begins; other positive apps keep working. Lunch and an available main Stop permit free usage. Returning from lunch retains zero configuration.
- Shared: exhaust an app before the shared budget. That app immediately begins its own hour and later recovers without refilling shared time. Even when all positive app caps are used, do not start a new group cooldown unless the shared budget is exhausted. Exhaust shared time during another app's partial allowance: all selected apps are blocked for one hour and reset afterward (zero configurations remain zero).
- Upgrade: update a running 0.5 shared cycle configured to 45 or 60 minutes. Its old remaining time and individual cooldown deadlines must survive. Settings shows the saved 30-minute next-cycle cap. Individual recovery does not apply it early; shared reset or manual Stop/Start adopts it. Preserve history and lunch phases.
- Lunch wheels: verify 12:00 AM=00:00, 12:00 PM=12:00 and 11:59 PM=23:59; independent hour/minute wrapping must not implicitly change AM/PM. Cancel/Back/outside discard drafts and restore Settings position. At 10 AM move 2 PM to 3 PM: today; choose 9 AM: tomorrow; after lunch starts: tomorrow. Preserve complete overnight lunch and cooldown.
- Notifications: keep 0.5 focused min(app, shared) countdown and icon rules, idle SocialPause icon and rows, Sleep Time hiding and Samsung setting. Use the corrected 0.7 dismissal behavior above. Zero rows say No allowance, never a made-up cooldown. Drawer continues charging the underlying app; screen lock and Home pause usage.


## Version 0.7 acceptance

These are acceptance steps, not a claim that the physical phone has passed them. Record results separately in `VALIDATION.md`.

- **Lunch notification regression:** with Sleep Time inactive, start monitoring in Individual mode, start manual lunch, swipe away its ordinary notification, let the unrestricted hour and following blocked hour finish, then open a selected app. Verify the focused expanded timer counts down, the correct icon is shown, and Samsung displays the live status-bar countdown. Repeat with a swipe during the lunch cooldown, with no dismissal, with an idle-notification swipe, and in Shared mode. Include early Stop lunch and a delayed callback around cooldown completion; use fake-clock cases for exact timing.
- **Intentional live dismissal:** dismiss an actual focused live countdown. The ordinary notification returns but no live chip is requested during the rest of that run, including after lunch and app switches. After Stop becomes available, Stop/Start clears suppression and permits promotion. Older ambiguous dismissal records also clear only at the next Start; upgrading alone must not erase them. Stale callbacks from an earlier run must not suppress the new run.
- **Start/Stop:** before Start, Home explains the six-hour lock. Start disables and greys out Stop, with “Stop available in HH:MM:SS.” Repeated Start calls do not refresh allowances or extend the lock. At 05:59:59 elapsed Stop is still disabled; at exactly 06:00:00 it enables. Monitoring must not end automatically. Use fake clocks for the exact boundary and include one real elapsed-time device check without changing production constants.
- **Time and lunch:** leave selected apps, lock the screen, enter Sleep Time, use a cooldown, start manual lunch, and press Stop lunch. The six-hour deadline must not move. Clock or timezone changes must not shorten it. Mode/app/allowance edits remain unavailable while running. After the deadline, Stop works normally; the next Start begins a full new lock.
- **Recreation and recovery:** recreate the activity, close/reopen the dashboard, remove it from Recents, and recreate the process without Force stop. Preserve the remaining lock and timers. Separately use Android App info → Force stop, then launch SocialPause: it must remain stopped until Start. Reboot during usage, lunch and cooldown: monitoring remains stopped and notifications/alarms stay absent until Start. Keep history, schedule, lunch endpoints and daily manual eligibility. API 35+ startup evidence must match the current process; older or unavailable records must not be mistaken for a new Force stop.
- **Upgrade/UI:** install 0.7.0/code 9 over 0.6 with the existing key during partial usage, cooldown and lunch. Preserve all existing state and do not invent a lock for that active run. Check the pre-Start notice, disabled Stop, countdown and enabled Stop in light/dark mode, large text and TalkBack. Verify the label updates while Home stays open. Retain all previous theme, timer, Insights and app-picker checks.

Emulator checks can establish notification payloads, chronometers, promotion requests and startup/recovery handling. They cannot certify the Samsung live pill, One UI dismissal behavior or battery restrictions. Keep those phone observations explicit.
